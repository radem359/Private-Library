@model PrivateLibrary.Models.AUTHOR

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>

@using (Html.BeginForm("DeleteInBase", "Authors"))
{
    @Html.AntiForgeryToken()
    <div>
        <h4>AUTHOR</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.NAMEOFATUTHOR)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NAMEOFATUTHOR)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.NATIONALITY.NAMEOFNATIONALITY)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NATIONALITY.NAMEOFNATIONALITY)
            </dd>

        </dl>



        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn-primary" value="@Model.IDOFAUTHOR" name="id">DELETE</button>
            </div>
        </div>

    </div>
}
    @Html.ActionLink("Back to List", "Index")
